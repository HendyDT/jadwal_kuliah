<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jadwalkuliah.jadwal_kuliah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Jadwal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SeninActivity" />
        <activity android:name=".SelasaActivity" />
        <activity android:name=".RabuActivity" />
        <activity android:name=".KamisActivity" />
        <activity android:name=".JumatActivity" />
        <activity android:name=".TambahJadwal" />
        <activity android:name=".EditJadwal" />
        <activity android:name=".LihatJadwal"></activity>
    </application>

</manifest>